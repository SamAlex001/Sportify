export const Home = () => {

    


    return (  
        <div>
            {username && (
                <h1>
                    User Logged In
                </h1>
            )}
            <h1>Home Page</h1>
        </div>
    )
}